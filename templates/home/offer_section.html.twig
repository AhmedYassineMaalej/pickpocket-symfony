<section class="offer-section">
    <h1 class="offer-title">{{ title }}</h1>
    <div class="offer-container">
        {% for offer in offers %}

        {{ render(controller('App\\Controller\\ProductCardController::renderProductCard', {'offer': offer})) }}

        {% endfor %}
    </div>
</section>
